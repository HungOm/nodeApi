{
  "name": "node-api",
  "description": "description",
  "authors": "H.Om",
  "version": "1.0.0",
  "scripts":{
    "start": "node index.js",
    "cluster": "node index-cluster.js",
    "dev": "env $(cat .env | grep -v \"#\" | xargs) yarn start",
    "cluster-dev": "env $(cat .env | grep -v \"#\" | xargs) yarn cluster"

  }
  
} 

